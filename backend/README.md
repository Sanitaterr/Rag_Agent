# Backend

Minimal FastAPI + LangChain + LangGraph Agent backend.

Kept responsibilities:

- DeepSeek chat model initialization
- Single-node LangGraph Agent
- LangGraph MySQL checkpoint persistence
- `session_id` / `thread_id` context reuse
- SSE streaming chat endpoint only
